迭代器模式(Iterator Pattern)是java和.Net编程中非常常用的设计模式；
这种模式用于顺序访问集合对象的元素，不需要知道集合对象的底层表示

意图：提供一种顺序访问一个聚合对象中各个元素，而又无须暴露该对象的内部表示
主要解决：不同的 方式来遍历整个对象；
何时使用：遍历一个聚合对象；
如何解决：把元素之间游走的责任交给迭代器，而不是聚合对象。
关键代码：定义接口:hasNext,next
应用实例：Java中的iterator。


实现：创建一个书序导航方法的iterator接口和一个返回迭代器的Container接口。
实现了Container接口的实体类将负责实现Iterator.
