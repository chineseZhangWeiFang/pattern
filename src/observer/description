观察者模式：当对象间存在一堆多关系时，则使用观察者模式(Observer patter)
比如，当一个对象被修改时，则会自动通知依赖它的对象。
观察者模式属性行为模式

意图：定义对象间的一种一对多的依赖关系，当一个对象的状态发生改变时，所有依赖于它的对象
都得到通知并被自动更新

主要解决：一个对象状态改变给其他对象通知的问题，而且要考虑到
易用和低耦合，保证高度的写作。

实现：观察者模式使用了三个类Subject,Observer和Client。
Subject对象带有绑定观察者到client对象和从Client对象解绑观察者
的方法。